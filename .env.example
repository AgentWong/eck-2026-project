# GitHub Personal Access Token (repo scope) for ArgoCD to clone this private repo
# Option 1: Set explicitly
# GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option 2: Use gh CLI token (recommended if gh is authenticated)
# GITHUB_PAT=$(gh auth token)
